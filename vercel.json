{
    "version": 2,
    "builds": [
        {
            "src": "./package.json",
            "use": "@vercel/node",
            "config": {
                "serverFiles": ["api/**"]
              }
        }
    ],
    "routes": [
        {
          "src": "/(.*)",
          "dest": "/"
        }
      ]
}